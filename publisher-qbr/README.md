# Publisher QBR Notes

Publisher-specific notes live here when they do not belong to the active extension or runner modules.

The active runtime is:

```text
Chrome extension -> local Publisher QBR runner -> qbr-pptx-service
```
