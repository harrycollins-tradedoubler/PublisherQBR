# Retired n8n Workflow Archive

This directory keeps old Publisher QBR n8n workflow exports for historical reference and emergency recovery only.

These workflows are not part of the active local runtime. The active Publisher QBR flow is:

```text
Chrome extension -> local Publisher QBR runner -> qbr-pptx-service
```

Do not use these exports as default setup, deployment, routing, or local runtime material. If a workflow needs to be restored, copy it out of this archive intentionally and document why the retired path is being reintroduced.
